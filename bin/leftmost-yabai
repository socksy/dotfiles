#!/usr/bin/env bash
CURRENT_X="$(timeout 0.3 yabai -m query --windows --window | jq '.frame.x')" || exit 1
timeout 0.3 yabai -m query --displays | jq -e "any(.frame.x <= $CURRENT_X and $CURRENT_X <= .frame.x+50)"
